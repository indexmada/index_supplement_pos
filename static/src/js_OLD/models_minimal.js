/** @odoo-module */

console.log('🚀 POS Models (Minimal Version) loaded');

// Version minimale sans patches pour éviter les conflits au setup
// Les modèles seront restaurés après les tests de base

console.log('✅ POS Models (Minimal) - No patches applied');
